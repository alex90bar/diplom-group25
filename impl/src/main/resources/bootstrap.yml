spring:
  application:
    name: microservice-person

  cloud:
    kubernetes:
      secrets:
        sources:
          - name: microservice-person
      config:
        sources:
          - name: library-core
          - name: microservice-person
      reload:
        enabled: true

management:
  endpoint:
    restart:
      enabled: true
    health:
      enabled: true
    info:
      enabled: true
    prometheus:
      enabled: true
